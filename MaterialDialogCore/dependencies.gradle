def globalConfiguration = rootProject.extensions.getByName("ext")
ext.versions = [
        minSdk            : globalConfiguration["androidMinSdkVersion"],
        compileSdk        : globalConfiguration["androidCompileSdkVersion"],
        buildTools        : globalConfiguration["androidBuildToolsVersion"],
        publishVersion    : '0.9.4.7',
        publishVersionCode: 177,
        gradlePlugin      : '3.0.0-beta2',
        spotlessPlugin    : '3.5.1',

        supportLib        : globalConfiguration["supportVersion"],
        mdProgressBar     : '1.4.1',
        butterKnife       : '8.8.1'
]
